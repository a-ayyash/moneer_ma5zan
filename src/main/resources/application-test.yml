erver:
  port: 8081

logging:
  level:
    org.springframework.web: debug

spring:
  profiles: test
  datasource:
    jdbcUrl: jdbc:postgresql://localhost:5432/moneer_qa
    username: ahmad
    password:
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: 'create'
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
